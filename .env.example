# Google Analytics 4 Measurement ID (e.g. G-XXXXXXXXXX).
# Analytics only loads when this is set. Configure it in the build environment
# (Cloudflare project settings) and, optionally, in a local .env for testing.
NUXT_PUBLIC_GA_ID=
